<div id="search-result-with-hits" class="govuk-grid-row">
  <% if @search_result.facet_filter_statistics.any? %>
    <div class="govuk-grid-column-one-third">
      <%= render 'beta/search_results/sidebar', guide: @search_result.guide %>
    </div>
    <div class="govuk-grid-column-two-thirds">
      <% if @search_result.multiple_headings_view? %>
        <%= render partial: 'beta/search_results/multiple_headings_search_results' %>
      <% else %>
        <%= render partial: 'beta/search_results/search_results' %>
      <% end %>
    </div>
  <% else %>
    <div class="govuk-grid-column-full">
      <% if @search_result.multiple_headings_view? %>
        <%= render partial: 'beta/search_results/multiple_headings_search_results' %>
      <% else %>
        <%= render partial: 'beta/search_results/search_results' %>
      <% end %>
    </div>
  <% end %>
</div>
