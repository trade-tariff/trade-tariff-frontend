<%= render 'measures/meursing_form' if grouped_measures_presenter.show_meursing_form? %>

<% grouped_measures_presenter.xi_sections.each do |section| %>
  <%= render partial: 'measures/grouped/table', locals: section %>
<% end %>
