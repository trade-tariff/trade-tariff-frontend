<%= card(
      title:,
      href: url,
      description:,
      status: local_assigns[:status],
      status_colour: local_assigns.fetch(:status_colour, 'magenta'),
      link_options: local_assigns.fetch(:link_options, {}),
    ) %>
