<% content_for :title, @title %>

<% if current_step.key.in?(%w[start end]) %>
  <%= render current_step.key, current_step: current_step, wizard: wizard %>
<% else %>
  <%= render "form", current_step: current_step, wizard: wizard %>
<% end %>
